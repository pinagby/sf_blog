<!DOCTYPE html>
<html>
    <head>
        <meta charset="uft-8">
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
        <title>{% block title %}NANA{% endblock %}</title>
        <link rel="stylesheet" href="{{ asset('bundles/nanablog/css/base.css') }}" type="text/css" media="all" />
        {% block stylesheets %}{% endblock %}
        {% block scripts %}{% endblock %}
        <link rel="shortcut icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <div class="container">
        {% if app.session.has('user') %}
            <div class="header_bar">
                欢迎你，{{ app.session.get('user') }}！
                <a href="{{ path('logout') }}">退出登录</a>
            </div>
        {% else %}
            <div class="header_bar">
                <a href="{{path('login')}}">登陆</a>
                <a href="{{path('register')}}">注册</a>
            </div>
        {% endif %}
            
            
            <div class="header_pic">
                <img src="{{ asset('bundles/nanablog/images/NANA.png') }}" >
            </div>
            <div class="nav">
                <ul>
                    <li><a href="{{path('homepage')}}">首页</a></li>
                    <li><a href="{{path('diary')}}">日志</a></li>
                    <li><a href="{{path('photo')}}">相册</a></li>
                    <li><a href="{{path('music')}}">音乐</a></li>
                    <li><a href="{{path('aboutme')}}">关于我</a></li>
                </ul>
            </div>
            <div class="clear"></div>
            <div class="content">
                {% block content %}{% endblock %}
                {% block javascripts %}{% endblock %}
            </div>
            <div class="footer">
                <hr class="footer_line" />
                NANA @ 2011
            </div>
        </div>
    </body>
</html>

